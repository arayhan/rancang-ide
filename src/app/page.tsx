export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 p-8">
      <p className="font-mono text-xs uppercase tracking-[0.08em] text-accent">
        Rancang Ide
      </p>
      <h1 className="max-w-xl text-center text-4xl font-semibold">
        Validate first, then build.
      </h1>
      <p className="max-w-md text-center text-muted">
        Turn ideas into validated product blueprints — ready for an AI coding agent.
        Coming soon.
      </p>
    </main>
  );
}
