// Drizzle schema — single source of truth for the database.
// Tables land with their phases (profiles/projects in Phase 1, TASK-009).
// Migrations are generated in-repo via `drizzle-kit generate`, never in the dashboard.

export {};
